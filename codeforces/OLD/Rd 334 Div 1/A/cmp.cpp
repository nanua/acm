#include<bits/stdc++.h>
using namespace std;

int main(){
	while(1){
		system("./gen");
		system("./chk");
		system("./A");
		if (system("fc out cmp")) puts("Wa"), exit(0);
		else puts("Ac");
			
	}
}
